{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "prompt": {
      "type": "string",
      "description": "The prompt or instruction to continue the conversation with Claude Code"
    },
    "model": {
      "type": "string",
      "enum": ["sonnet", "opus"],
      "description": "Claude model to use"
    },
    "maxTurns": {
      "type": "number",
      "description": "Maximum number of conversation turns allowed"
    },
    "timeout": {
      "type": "number",
      "description": "Maximum time to wait for completion (in seconds)"
    },
    "projectPath": {
      "type": "string",
      "description": "The directory path where Claude Code should run"
    },
    "outputFormat": {
      "type": "string",
      "enum": ["structured", "messages", "text"],
      "description": "Choose how to format the output data"
    },
    "allowedTools": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Select which built-in tools Claude Code is allowed to use"
    },
    "disallowedTools": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Select which built-in tools Claude Code is explicitly blocked from using"
    },
    "additionalOptions": {
      "type": "object",
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "Whether to enable debug logging"
        },
        "fallbackModel": {
          "type": "string",
          "enum": ["", "sonnet", "opus"],
          "description": "Automatically switch to fallback model when primary model is overloaded"
        },
        "maxThinkingTokens": {
          "type": "number",
          "description": "Maximum number of thinking tokens"
        },
        "permissionMode": {
          "type": "string",
          "enum": ["default", "acceptEdits", "bypassPermissions", "plan"],
          "description": "How to handle permission requests for tool usage"
        },
        "systemPrompt": {
          "type": "string",
          "description": "Additional context or instructions for Claude Code"
        }
      }
    }
  },
  "required": ["prompt"],
  "additionalProperties": false
}